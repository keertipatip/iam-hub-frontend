import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // API routes are now handled by Next.js Route Handlers in app/api/
  // No proxy to Go backend needed.
}

export default nextConfig
